services:
  clarity:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clarity
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/clarity.db
    volumes:
      - clarity_data:/data
    restart: unless-stopped       

volumes:
  clarity_data:
